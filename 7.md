

## fixes and changes:

first lets make the def save ai made changes in the chatting interface to always ask[as default, and no option to switch to exeute every time, so not ui for the select dropdown]

And add a filesystem layer in between the r2 storage and the web app ai agent builder, so that the ai agent can use terminal cmd to let the ai easily use the agent tools by cmd etc, this should be a big implementation.

1.lets solve the problem of the file edting, now lets solve it by implementing a middle layer:once the user creates or opens a project and then it auto connects to the chat then, a local filesystem it made for the connected project, all the files, created, edited, deleted, imported, etc are managed on that local filesystem layer, and the pushed and saved to the user project r2 once the user confirms or save manually, for example: actions such as : user creates, deletes, moves, copies, renames, etc, files and folders manually in the builder page, and also actions like:import from device, import form general storage[like the images, audio, videos, 3d models generated , which are storred in the general storage of the user (if not connected to a project in the time of the assets generation, if connected then also store that asset to the general and then auto import to the connected project, but should not doudly stored the actual asset file(should be handled in the public urls only (one specific asset file is stored once and then if the user copies, imported to another , then should use the public urls of that assset , to maintain storage cost and limits)))] these type of actions are implemented to the r2 storage instantly, and then these actions: create, edit, delete, move, copy, rename etc files and folders by the ai , these changes made should be still in the local filesystem layer{in affect only the local file system}, and then pushed to the r2 user project storage when the user confirms of the changes [in the chatting interface, asks everytime]etc

and also even if the user confirms of the changes and then pushed to the r2 , also keep a copy of the recent file chnages[before it was changes, but should not stack only save the unchanged of the currently changes, so that the undo and redo btns work in the builder page, so that if the user makes any changes then if he undo or redo then it should be reverted and should be again pushed to r2]


