

## NEW FIXES AND NEW IMPLEMENTAIONS:

1.In the renderer page, when the user swtiches to the mobile view, then the ui which are added by js codes like bars, uis etc disappear, fix this issue, and also when the user switches to mobile view, when the render page reloads then it should save the user's prefered view mode[desktop or mobile].

2.when the user hovers over the project icon in the left side bar, and then when the user clicks on the open in new tab option, then the new builder page opened[in this page remove the leftsidebar, should be single builder page]

3.in the chattinginput, when the user writes a long txt prompt, then the input box should expand to fit the text, and when the user clicks on the send button, then the text should be sent, and the input box should collapse to its original size, and also the btns in the input should also adjust to the chnages in the input box size professionally.

4.in the builder page's txt file viewer when the user edits a file then it automatically saved as pushes to r2[however this should not be the case, user/agent edits a file(s), then apply affect to vfs->effect to the renderer ->[when user clicks on save or approve then the changes are saved to r2]]

5.also reduce the max user message bubble horizontal width to 60% of the current's width

6.in the chatting input element, when the user attaches a file form his/her device then, set the bg of the new increased height of the input element[when image is attached from device] to slight transparent liquid blur like effect.


7.in the chatting interface, when the user attaches/tags a file in the user input then currently it shows whte file icon[remove this] it should be only like @main.js,
and then in the agents response messages when it calls tools and does actions like create, edit,delete files/folders , then dont add the edit or create file icons, just show like [icon for the file(if js then js file icon, if txt then txt file icon, etc for every file types)], and also one file create/edited etc indication should not occupy a whole horizontal line, it should be like [js icon] main.js [lines added and lines removed, which each colours] (here next to it[not break line, if another file involved then [txt icon] main.txt [lines added and lines removed, which each colours] [css icon] styles.css [lines added and lines removed, which each colours]], as much as it fits in the container of the agent message response, if not fit then break to next line)



8.check if the web app system is ready for task like:

  1.create a new image like this @tagtoanimagefile, but the character should be one from this character's file @character.json[character.json contains a list of characters with their details like name, age, gender, designs etc], and then set the new generated image as the game's loading screen with a progressn bar in the btn which fills in 5 seconds with variable speed, the fill colour should be a gradient night cold blue and weak purple, with animated liquid effects.
  [expectation on how the web app would handle this task: 1.the agent understands the project, and the user inputs, understands the files attached/tagged, user image edit(txt + image-> image) to edit the given ref iamge with the character style and description(here the ai should improve the design descroption of the character  to be the best and much more detailed, [from head to toe]), and then submit the image edit task to the task and processes(to run in backgorund), and then when this is running, the agent should write the code to implement the loading screen with the progress bar, and then wait the image edit task to complete, and then add the path of the iamge to the loading image screen]

