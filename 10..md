## FIXING THE MODE SELECTION IN THE INPUT ELEMENTS:
1.FOR THE FAST MODE USE THESE MODELS AND SYSTEMS(here no orchastrator is used):
gemini-3.1-flash-lite[from gemini api][set thinking level to mid](if hit minute rate limit) -> gemini-3-flash[set thinking level to mid][from gemini api](if hit minute rate limit) -> gemini-2.5-flash[set thinking level to mid][from gemini api](if hit minute rate limit) ->Qwen 3 235B A22B Thinking 2507[from clod api] (here if rate limit then use the above two gemini models and then the qwen model till hit day limi, if all these hit day limit then switch to another key, i.e another gemini key and another clod key )



2.FOR THE AUTO MODE USE THESE MODELS AND SYSTEMS[use these as orchastrators:gemma-4-31b (DEF, IF RATE LIMIT, THEN USE THE DOWNWARD MODELS AS BACKUP, AND IF ALL DAY[NOT MINUTE, IF ALL MODELS MINUTE RATE LIMIT THEN, GIVE USER HEAVY TRAFFIC RETRY AGAIN ERROR] RATE LIMITED THEN CHANGE TO ANOTHER API)
   - gemma-4-26b 
   - gemma-3-27b, ](note: create a prompt for the orchastrator to be strict when choosing models for the user tasks and request, make a guide for the orchastrator on how to asign models for each tasks etc)
 
 HERE THE ORCHASTRATOR MODELS CAN SWITCH TO THESE MODELS:

 here the first payable prototype game should be generated by paralium another mode[like fast, auto etc], so the orchastrator has different acess to models before and after paralium builds the user's prototype playable game.

   
   ## Before the first prototype game[before paralium] the orchastrator has acess to these models:

   - gemini-3.1-flash-lite [from gemini api][set to thinking level low as def, but mid for slightly harder tasks and high for complex tasks eg, coding , long planning, assets generation descriptions etc](if hit minute rate limit) -> gemini-3.5-flash[set to thinking level low as def, but mid for slightly harder tasks and high for complex tasks eg, coding , long planning, assets generation descriptions etc][from gemini api](if hit minute rate limit) ->gemini-3-flash[set to thinking level low as def, but mid for slightly harder tasks and high for complex tasks eg, coding , long planning, assets generation descriptions etc][from gemini api](if hit minute rate limit) -> Qwen 3 235B A22B Thinking 2507[from clod api] (here if rate limit then use the above two gemini models and then the qwen model till hit day limi, if all these hit day limit then switch to another key, i.e another gemini key and another clod key )

   and also for the above if tinking level is set to high then:thinkingConfig.includeThoughts: true, to show the thought process[handle it in smaller texts in the ui carefully and professional, once the thinking is completed then the though prcess txt should be closed in a dropdown , which the user can open to see it again], also show the time taken to think in secs, excluding models: qwen 3 235B A22 Thinking 2507[from clod api]
   
   ## After the user has an initail playable game the orchastrator can use these below models:
   - gpt-5.3-codex [for heavy, complex tasks, and for debugging code, used when user hits the same error for multiple times, or face error next by next, do not use this model for light, easy coding, debudding tasks](from laysoai api provider)
   - claude-sonnet-4-5-20250929 [for heavy planning, complex multi step game mechanics, multi-dimensional, multi-layered tasks, for end-to-end game design, from game concept to asset generation instructions to full detailed game play walkthroughs](from laysoai api provider)
   - gemini-3.1-flash-lite [for coding tasks, small changes, multiple small changes etc, for small errors](from gemini api provider)
   - Qwen 3 235B A22B Thinking 2507 [BACKUP, for coding tasks, small changes, multiple small changes etc, for small errors](from clod api provider)


3.PARALIUM MODE:

 This is an agentic mode that builds the user desired game in the fastest and using the most premium models, and agents running in parallel [here no orchastrator models are included, as this process/mode run by steps, so different steps have static/specified models]

 Here models include:

 1.gemini-3.1-flash-lite[from gemini api](use for chatting with the user about the game idea, coming up with questions to ask user related to the game idea, more questions and then structuring, organising the info/data and then)

 2.GPT-5.4[from skillboss api provider](to take images as input and then describes them in very high details, used for game screens, layout images, items, heros, ui btns, etc)

 3.claude opus 4.7 thinking[from laysoai api provider](for heavy planning, complex multi step game mechanics, multi-dimensional, multi-layered tasks, for end-to-end game design, from game concept to asset generation instructions to full detailed game play walkthroughs)

 4.GPT-5.3-codex[from laysoai api](for heavy, complex tasks, and for debugging code, used when user hits the same error for multiple times, or face error next by next, do not use this model for light, easy coding, debudding tasks)


 5.qwen 3 235B A22B Thinking 2507[from clod api](used as backup for gemini-3.1-flash-lite)


## THEN THE MODEL SELECTION MODE:

also make this functional:


                          'gemini-3-flash': 'Gemini 3 Flash', [shpuld use gemini api key]
                          'gemini-3.5-flash': 'Gemini 3.5 Flash',[shpuld use gemini api key]
                          'deepseek-v4-pro': 'Deepseek V4 Pro',[should use laysoai api]
                          'gemini-3-pro-preview': 'Gemini 3 Pro Preview',[should use laysoai api]
                          'gpt-5.2': 'GPT 5.2',[should use laysoai api]
                          'claude-opus-4.5': 'Claude Opus 4.5',[should use laysoai api]
                          'claude-sonnet-4.5': 'Claude Sonnet 4.5',[should use laysoai api]
                          'gpt-5.3-codex': 'GPT-5.3-Codex'[should use laysoai api]
                          'qwen-3-235B-a22b-thinking-2507'[should use clod api]

 